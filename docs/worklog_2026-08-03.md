# MEMOry 작업 기록 — 2026-08-03

## 작업 요약

- 메모 휴지통 상세 화면에 복원 기능 추가
- 메모 영구 삭제와 휴지통 이동 확인 문구를 구분
- 삭제된 메모에서는 수정·중요 메모 관련 동작을 숨기도록 정리
- 일정 삭제 방식을 즉시 영구 삭제에서 휴지통 이동 방식으로 변경
- 일정 데이터에 삭제 상태(`isDeleted`)를 저장하고 기존 일정 데이터와의 호환성 유지
- 휴지통 목록에서 삭제된 일정의 제목, 날짜, 시작 시간 또는 종일 정보를 표시
- 휴지통에서 일정 복원 및 영구 삭제 기능 추가
- 삭제된 일정을 캘린더, 일정 팝업, 브라우저 알림 및 요약 알림 대상에서 제외
- 모바일 하단 탐색에 휴지통 메뉴 추가
- 모바일 휴지통에서 삭제된 메모와 일정을 각각 복원하거나 영구 삭제할 수 있도록 구현
- 휴지통 상태 전환 로직을 `src/utils/trash.ts`로 분리하고 회귀 테스트 추가
- 일정 팝업에 오늘·내일·모레 일정과 알림 일정을 카드 또는 표 형식으로 표시
- 일정 우선순위 표시 컴포넌트 추가
- 브라우저 일정 알림과 일간·주간 요약 알림 기능 추가
- 설정 화면에 알림 권한, 일간 요약 시간, 주간 요약 요일·시간 설정 추가
- 알림 설정과 일정 상태를 Firebase 클라우드 저장 데이터에 포함

## 검증

- `npm.cmd run lint`: 통과
- `npm.cmd run test:vitest`: 19개 테스트 파일, 79개 테스트 통과
- `npm.cmd run test:unit`: 2개 테스트 스위트, 13개 테스트 통과
- `npm.cmd run test:e2e`: 데스크톱·모바일 4개 시나리오 통과
- `npm.cmd run build`: 통과
- `npm.cmd run deploy:check`: 통과
- 배포 사전검증: Hosting/Data 프로젝트 및 운영 번들 검증 통과

## GitHub 반영

- 기능 브랜치: `agent/add-trash-restore-and-notifications`
- 기능 브랜치 커밋: `937944d` (`Add trash restore and schedule notifications`)
- Pull Request: `#1 Add trash restore and schedule notifications`
- PR URL: `https://github.com/mohenz/personalMemo/pull/1`
- 병합 대상: `main`
- 병합 방식: squash
- `main` 반영 커밋: `1c173ef` (`Add trash restore and schedule notifications (#1)`)

## 운영 배포

- Firebase Hosting 대상: `archive-store-fae71`
- 데이터/Auth/Storage 프로젝트: `archive-store-v2-3d020` 유지
- 배포 범위: Firebase Hosting만
- 운영 번들: `assets/index-DNv6mi4X.js`
- 운영 URL: `https://archive-store-fae71.web.app`
- 배포 상태: `2026-08-03 22:15 KST` 완료
- 운영 검증: HTTP 200 및 배포 번들·데이터 프로젝트 검증 통과

## 최종 상태

- 메모·일정 휴지통 복원 기능이 데스크톱과 모바일 운영 환경에 반영됨
- 일정 팝업, 우선순위 표시, 브라우저 알림, 일간·주간 요약 알림이 운영 환경에 반영됨
- 로컬 `main`과 `origin/main`이 `1c173ef` 기준으로 동기화됨
