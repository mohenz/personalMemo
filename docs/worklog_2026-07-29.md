# MEMOry 작업 기록 — 2026-07-29

## 작업 요약

- 사이드바 프로필 이미지를 클릭하면 스플래시를 다시 실행하지 않고 모든 메모 대시보드로 이동하도록 변경
- 스플래시 수동 재실행 버튼을 제거해 최초 구동 시에만 표시되도록 정리
- 사이드바 메뉴 글자 크기를 14px로 통일하고 세로 패딩을 축소
- 대시보드 상단 헤더의 설정 버튼 제거
- 선택일 일정 카드와 주간·일간 시간표 일정 블록을 `시간 → 일정명` 순서로 변경
- 월간 캘린더에서 메모 카드 대신 일정 시간과 일정명을 표시하고 글자 크기를 9px로 조정
- 월간·주간·일간 캘린더 본문은 일정 전용으로 구성
- 선택일 우측 패널에서는 해당 날짜의 일정과 메모를 함께 표시
- 캘린더 검색을 일정 전용 검색으로 변경하고 캘린더 내부의 새 메모 버튼 제거
- 일정 표시 순서를 검증하는 `TimeGrid` 테스트와 캘린더 회귀 테스트 보강
- 일정 등록·수정 화면에 매주 반복 옵션과 월~일 복수 요일 선택 기능 추가
- 반복 일정의 선택 요일별 캘린더 표시와 시작일·종료일 범위 처리를 구현
- 반복 종료일을 선택 입력으로 제공하고 달력 열기 버튼을 추가
- 반복 일정 유틸리티·캘린더 표시·입력 모달 회귀 테스트 보강

## 검증

- `npm run lint`: 통과
- `npm run test:vitest`: 15개 테스트 파일, 70개 테스트 통과
- `npm run test:unit`: 2개 테스트 스위트, 13개 테스트 통과
- `npm run test:e2e`: 데스크톱·모바일 4개 시나리오 통과
- `npm run build`: 통과
- `npm run deploy:check`: 통과

## 배포

- Git 브랜치: `main`
- Git 원격: `https://github.com/mohenz/personalMemo.git`
- Firebase Hosting 대상: `archive-store-fae71`
- 데이터/Auth/Storage 프로젝트: `archive-store-v2-3d020` 유지
- 배포 범위: Firebase Hosting만
- 소스 커밋: `dd0ab64` (`Add weekly recurring schedules`)
- 운영 번들: `assets/index-C3_I0vDV.js`
- 운영 URL: `https://archive-store-fae71.web.app`
- 배포 상태: `2026-07-29 05:13 KST` 완료, HTTP 200 및 운영 자산 검증 통과

## 중앙 기록 동기화

- `project_control/states/personal_memo_current.md`에 오늘 UI·캘린더 개선, 매주 반복 일정, 검증 및 운영 배포 결과를 반영
- `project_control/states/project_control_current.md`에 MEMOry 중앙 기록 갱신 사실을 반영
- `project_control`의 변경사항을 `origin/main`에 푸시 완료
